
module.exports = {
    types: require('./types'),
    shows: require('./shows'),
    lists: require('./lists'),
    views: require('./views'),
    filters: require('./filters'),
    rewrites: require('./rewrites'),
    updates: require('./updates')
};

// Bind event handlers
// require('./events')
